apiVersion: v1
kind: Service
metadata:
  name: {{ include "secretsmanagerui-helm-chart.fullname" . }}
  labels:
    {{- include "secretsmanagerui-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secretsmanagerui-helm-chart.selectorLabels" . | nindent 4 }}
